// Placeholder interfaces for future data structures
// Add interfaces here as needed for your app's data models

export interface User {
  username: string;
  // Add other user properties as needed
}

export interface Practice {
  id: string;
  // Add other practice properties as needed
}

export interface Group {
  id: string;
  // Add other group properties as needed
}

